# 拒绝直接访问 src/ 目录下的 PHP 源码（M3 升级 Apache 2.4 双语法）
# 这些文件必须通过 index.php 入口加载，直接访问应被禁止

<IfModule mod_authz_core.c>
    # Apache 2.4+
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    # Apache 2.2 兼容（phpStudy 旧版等）
    Order deny,allow
    Deny from all
</IfModule>
