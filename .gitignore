# 操作系统
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# 日志
*.log
npm-debug.log*

# 依赖
node_modules/

# 临时文件
*.bak
*.tmp
*.temp

# 备份
storage.bak/
uploads.bak/

# Playwright
.playwright-mcp/
# Playwright MCP 默认保存到 cwd 的截图（调试用）
/*.png
!docs/**/*.png

# Claude
.claude/
.omc/

# 本地 Claude 上下文（含部署服务器等敏感信息，不入库）
CLAUDE.local.md

# OMP 全局 agent 规则（不属于本项目，跨仓库复用）
/AGENTS.md

# 运行时配置（敏感，不入库）
.env
.user.ini

# 运行时数据（SQLite 数据库、JSON 备份）
storage/fileshare.db
storage/data.json.bak
storage/upload_log.json.bak
uploads/!*
phpinfo.php

# 本地工具脚本（可能含服务器路径/凭据，不入库）
/tools/

# Composer 依赖与构建产物
/vendor/
/composer.lock
